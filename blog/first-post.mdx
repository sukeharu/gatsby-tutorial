---
title: "First Post"
date: "2021-06-22"
slug: "first-post"
---

# スコープ

スコープとは変数などオブジェクトの「アクセス可能範囲」のこと。原則として内側のスコープから外側のスコープを参照することはできるが、その逆はできない。範囲の大きいスコープから順に、JavaScirptには3つのスコープがある。例えば、関数スコープからグローバルスコープの参照はできるが、グローバルスコープから関数スコープの参照はできない。

1. グローバルスコープ
2. 関数スコープ
3. ブロックスコープ