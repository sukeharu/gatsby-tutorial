---
title: "Third Post"
date: "2021-09-17"
slug: "third-post"
hero_image: "./pexels-ash-376464.jpg"
hero_image_alt: "object's properties"
hero_image_credit: "third person"
hero_image_credit_link: "https://globe.asahi.com/feature/11034327"
---

# オブジェクトのプロパティを列挙する方法

オブジェクトのプロパティは `for...in` で簡単に列挙することはできない（iterableでないということ）。オブジェクトのプロパティを列挙するには次のようなスクリプトを書く。

~~~javascript
for (const key in object) {
  if (Object.hasOwnProperty.call(object, key)) {
    const element = object[key];
    
  }
}
~~~

なぜこうするか？　`hasOwnProperty()` というプロパティ名は保護されていないので上書き可能なため、万全を期すには `for...in` を使ってプロパティを列挙するよりも、`Object` の静的メソッドを使ったほうが確実だから。